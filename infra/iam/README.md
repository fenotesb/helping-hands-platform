# IAM (Helping Hands)

This folder contains IAM policies used by the Helping Hands platform.

- `policies/lambda-trust-policy.json`
  Trust policy that allows AWS Lambda to assume the execution role.

These files contain no secrets and are safe to commit.
